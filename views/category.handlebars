<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Fund My Project!</title>
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/style.css">
</head>


<body>
  <h2 id="top-picks">{{category.dataValues.category_name}}</h2>
  <main class="container container-fluid mt-5">
  {{#each category.products}}
    <a href="/product/{{dataValues.id}}" class="product-link">
      <div id="prodcard" class="product-card" style="background-image: url('{{dataValues.image}}');
      background-size: cover;">
        <h2 class="product-title">{{dataValues.product_name}}</h2>
        <p class="condition">Condition: {{dataValues.condition}}</p>
      </div>
    </a>
  {{/each}}
</main>


  <footer>
  </footer>
  </div>

  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>