<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{product_name}}</title>
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <div class="container">
    <div class="row">
      <!-- Left Column: Product Photo and Star Rating -->
      <div class="col-md-6">
        <div class="product-image">
          <img src="/path/to/product-image.jpg" alt="{{product_name}} Image">
        </div>
        <div class="product-rating">
          <!-- Display star rating here -->
          <div class="star-rating">
            {{#if average_rating}}
            {{!-- Display stars based on average_rating --}}
            {{#stars average_rating}}
            <span class="star">&#9733;</span>
            {{/stars}}
            {{#emptyStars average_rating}}
            <span class="star">&#9734;</span>
            {{/emptyStars}}
            {{else}}
            {{!-- Display no stars if no average rating --}}
            <span class="star">No ratings yet</span>
            {{/if}}
          </div>
        </div>
        <div class="product-description">
          <!-- Display product description here -->
          {{description}}
        </div>
      </div>

      <!-- Right Column: Product Details and Add to Cart -->
      <div class="col-md-6">
        <div class="product-details">
          <h1>{{product_name}}</h1>
          <p>Price: ${{price}}</p>
          <p>Condition: {{condition}}</p>
          <p>How Long Available: {{how_long_available}}</p>
        </div>
        <div class="add-to-cart">
          <button class="btn btn-primary">Add to Cart</button>
        </div>
        <div class="product-reviews">
          <!-- Display product reviews here -->
        </div>
        <div class="add-review">
          <!-- Form for adding reviews -->
          <h2>Add a Review</h2>
          <form class="form review-form">
            <div class="form-group">
              <label for="review-text">Review:</label>
              <textarea class="form-input" id="review-text" name="review-text"></textarea>
            </div>
            <div class="form-group">
              <label for="star-rating">Star Rating:</label>
              <!-- Include star rating input here -->
              <select class="form-input" id="star-rating" name="star-rating">
                <option value="1">1 Star</option>
                <option value="2">2 Stars</option>
                <option value="3">3 Stars</option>
                <option value="4">4 Stars</option>
                <option value="5">5 Stars</option>
              </select>
            </div>
            <div class="form-group">
              <button type="submit" class="btn btn-primary">Submit Review</button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>

  <script src="/js/product.js"></script>
</body>

</html>


